let idade = 18;
let mensagem;

idade >= 18 ? mensagem = 'Maior de idade'  : mensagem = 'Menor de idade';
console.log(mensagem);

let numero = -5;

let resultado = numero > 0 ? 'Positivo' : (numero < 0 ? 'Negativo' : 'Zero');
console.log(resultado)